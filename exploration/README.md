## Exploratory Notebooks

This subdirectory contains Jupyter notebooks used to test library dependencies and run basic models to diagnose potential downstream issues.

- `FINAL_initial_exploration.ipynb`: Run basic models
- `test_notebook.ipynb`: Test library dependencies
